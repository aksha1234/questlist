# Server-only configuration. Never prefix these with VITE_ or commit a real key.
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
# Optional local API port. Render supplies PORT automatically.
QUESTLIST_API_PORT=8787
